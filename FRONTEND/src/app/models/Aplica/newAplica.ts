export class newaplica {
    enom:string;
    tit:string;
    pcor:string; 
    aest:string;
    adesc:string;
    ana:string;

    constructor(enom:string, tit:string, pcor:string, aest:string, adesc:string, ana:string) {
        this.enom = enom
        this.tit = tit
        this.pcor = pcor
        this.aest = aest
        this.adesc = adesc
        this.ana = ana
    }
}