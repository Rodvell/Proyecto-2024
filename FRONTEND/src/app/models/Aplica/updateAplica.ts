export class updateaplica {
    enom:string;
    tit:string;
    pcor:string;
    aest:string;

    constructor(enom:string, tit:string, pcor:string, aest:string) {
        this.enom = enom;
        this.tit = tit;
        this.pcor = pcor;
        this.aest = aest;
    }
}