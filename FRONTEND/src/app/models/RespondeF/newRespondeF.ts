export class newrespondef {
    enom:string;
    tit:string;
    id:number; 
    pcor:string; 
    resp:string

    constructor(enom:string, tit:string, id:number, pcor:string, resp:string) {
        this.enom = enom;
        this.tit = tit;
        this.id = id;
        this.pcor = pcor;
        this.resp = resp
    }
}