export class newperfil_p {
    pnom:string;
    ape:string; 
    dpi:string; 
    pcor:string; 
    pass:string;

    constructor(pnom:string, ape:string, dpi:string, pcor:string, pass:string) {
        this.pnom = pnom;
        this.ape = ape;
        this.dpi = dpi
        this.pcor = pcor;
        this.pass = pass;
    }
}