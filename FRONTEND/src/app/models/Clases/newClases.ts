export class newclases {
    cnom:string; 
    id: number; 
    pcor: string;
    est: string;

    constructor(cnom:string, id:number, pcor:string, est:string) {
        this.cnom = cnom;
        this.id = id;
        this.pcor = pcor;
        this.est = est;
    }
}