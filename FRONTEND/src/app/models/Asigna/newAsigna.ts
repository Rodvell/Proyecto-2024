export class newasigna {
    pcor:string;
    cnom:string;
    est:string;

    constructor(pcor:string, cnom:string, est:string) {
        this.pcor =pcor;
        this.cnom = cnom
        this.est = est;
    }
}